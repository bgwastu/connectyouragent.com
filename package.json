{
  "name": "cya",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "sh scripts/build-bridge.sh",
    "start": "bun server/index.ts",
    "dev": "bun run build && bun run start",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "verify": "bun run typecheck && bun run build && bun test"
  }
}
