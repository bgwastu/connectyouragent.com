{
  "name": "cya",
  "version": "0.1.0",
  "module": "server/src/index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  },
  "description": "Connect Your Agent — temporary bridge for AI shell access",
  "scripts": {
    "start": "bun run server/src/index.ts",
    "build:agents": "bun run agent/build.ts",
    "dev": "bun --hot run server/src/index.ts"
  },
  "type": "module"
}
